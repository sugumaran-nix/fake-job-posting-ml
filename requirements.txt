# JobGuard v7 — Python 3.11 compatible, all free-tier
# ──────────────────────────────────────────────────────
# Core
flask==3.0.3
gunicorn==22.0.0
python-dotenv==1.0.1

# ML
scikit-learn==1.5.2
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
nltk==3.9.1

# Plotting (for train.py only)
matplotlib==3.9.2
seaborn==0.13.2

# Rate limiting (gracefully skipped if absent)
flask-limiter==3.8.0

# Optional — tighter error tracking on Render
# sentry-sdk[flask]==2.17.0
