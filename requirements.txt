# JobGuard v7 — Production Edition
# All versions pinned for reproducibility (research: sklearn models break across versions)
# Python 3.11.9  (see runtime.txt)
# ──────────────────────────────────────────────────────────────────────

# ── Core web ───────────────────────────────────────────────────────────
flask==3.0.3
gunicorn==22.0.0
python-dotenv==1.0.1

# ── Security (best practice — OWASP 2025) ─────────────────────────────
flask-talisman==1.1.0       # HSTS, X-Frame-Options, X-Content-Type-Options
flask-wtf==1.2.2            # CSRF protection for all POST forms
flask-limiter==3.8.0        # API rate limiting

# ── ML ────────────────────────────────────────────────────────────────
scikit-learn==1.8.0         # PIN — models break across sklearn versions
numpy==1.26.4               # PIN — sklearn depends on exact numpy ABI
scipy==1.13.1
joblib==1.4.2               # Official sklearn serialization recommendation
nltk==3.9.1
pandas==2.2.3               # train.py only

# ── Database ──────────────────────────────────────────────────────────
psycopg2-binary==2.9.10     # PostgreSQL driver (Supabase / Neon / Railway)
                             # Remove if only using SQLite (saves ~10MB)

# ── Plotting (train.py only — not needed at runtime) ──────────────────
matplotlib==3.9.2
seaborn==0.13.2

# ── BERT (run bert_finetune.py in GitHub Codespaces, not on Render) ───
# Only needed for training. Render only needs onnxruntime for inference.
# transformers==4.44.2    # fine-tuning only — NOT in Render requirements
# torch==2.4.1            # fine-tuning only — NOT in Render requirements
# datasets==2.21.0        # fine-tuning only — NOT in Render requirements

# ── ONNX inference (add this to Render) ──────────────────────────────
onnxruntime==1.19.2       # ~12MB — needed on Render for BERT inference
