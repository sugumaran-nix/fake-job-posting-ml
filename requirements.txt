# JobGuard — Production Edition
# Python 3.11  (see runtime.txt)
# ──────────────────────────────────────────────────────────────────────

# ── Core web ───────────────────────────────────────────────────────────
flask==3.0.3
gunicorn==22.0.0
python-dotenv==1.0.1

# ── Security ───────────────────────────────────────────────────────────
flask-talisman==1.1.0
flask-wtf==1.2.2
flask-limiter==3.8.0

# ── ML ────────────────────────────────────────────────────────────────
# FIX: scikit-learn 1.8.0 does not exist — latest stable is 1.5.x.
# Pin to 1.5.2 which is the last stable release as of late 2024.
scikit-learn
numpy
scipy
joblib==1.4.2
nltk==3.9.1
pandas==2.2.3
# FIX: contractions is used in utils/preprocessing.py (graceful fallback
# if absent, but listing it here ensures it is installed in all environments).
contractions==0.1.73

# ── Database ──────────────────────────────────────────────────────────
psycopg2-binary==2.9.10

# ── BERT inference (Render / production) ──────────────────────────────
# onnxruntime: INT8 dynamic quantisation with quantize_dynamic()
# produces a single self-contained .onnx (no .data sidecar).
torch==2.3.1+cpu
--extra-index-url https://download.pytorch.org/whl/cpu
# huggingface-hub: download_bert.py fetches the ONNX model at startup.
huggingface-hub==0.24.6
# transformers: tokenizer only (no torch dependency at inference time).
transformers==4.44.2

# ── Plotting (train.py only — not needed at inference) ─────────────────
matplotlib==3.9.2
seaborn==0.13.2
